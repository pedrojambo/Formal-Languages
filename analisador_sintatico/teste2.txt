fn aluga (gustavo bool) : int {
    var x : int;
    var y : float = -1;
};

fn paga (pedro bool) : float {
    pedro = false;
    return 0;
};

//comentario

fn vende (valor float) : bool {
    bruna = true;
    while (x == y) {
        if (true) {
            pedro = 3;
            return 0;
        }
        else{
            gustavo = 2 + 2;
        };
    };
    if (bruna != gustavo) { return 0; };
};

/* oi /* comentario dentro de comentario */ tchau */